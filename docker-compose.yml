services:
  valkey:
    image: valkey/valkey:8.0.2-alpine
    command: valkey-server --save "" --appendonly no
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "valkey-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10
